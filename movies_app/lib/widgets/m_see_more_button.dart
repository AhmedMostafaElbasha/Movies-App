import 'package:flutter/material.dart';

class MSeeMoreButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return InkWell(
      onTap: () {},
      child: Text(
        'SEE ALL',
        style: TextStyle(
          color: Colors.white60,
        ),
      ),
    );
  }
}
